name: DevOps Pipeline (Terraform + Python)

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Terraform Init
      working-directory: aws_terraform
      run: terraform init

    - name: Terraform Plan
      working-directory: aws_terraform
      run: terraform plan

    - name: (Optional) Apply Terraform Automatically
      if: github.ref == 'refs/heads/main'
      working-directory: aws_terraform
      run: terraform apply -auto-approve

    - name: Run Python AWS Automation
      run: python scripts/aws/s3_backup.py
      env:
        S3_BUCKET: ${{ secrets.S3_BUCKET }}

    - name: Run Kubernetes Pod Checker
      run: python scripts/k8s/list_pods.py


  deploy_to_eks:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Configure AWS creds
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}
      - name: Setup kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'
      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig --region ${{ secrets.AWS_REGION }} --name ${{ secrets.EKS_CLUSTER_NAME }}
      - name: Apply k8s manifests
        run: |
          kubectl apply -f k8s_manifests/


#Make sure to set the GH secrets: AWS_ACCESS_KEY_ID, #AWS_SECRET_ACCESS_KEY, AWS_REGION, EKS_CLUSTER_NAME.

